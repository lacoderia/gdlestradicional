<%= form_for(@influencer) do |f| %>
  <% if @influencer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@influencer.errors.count, "error") %> prohibited this influencer from being saved:</h2>

      <ul>
      <% @influencer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :is_especial %><br>
    <%= f.check_box :is_especial %>
  </div>
  <div class="field">
    <%= f.label :video_url %><br>
    <%= f.text_field :video_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
